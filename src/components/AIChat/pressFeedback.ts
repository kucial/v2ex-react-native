import { StyleSheet } from 'react-native'

export const pressFeedbackStyles = StyleSheet.create({
  compact: {
    opacity: 0.68,
    transform: [{ scale: 0.92 }],
  },
  regular: {
    opacity: 0.78,
    transform: [{ scale: 0.98 }],
  },
})
